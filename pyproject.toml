[build-system]
# Build dependencies for the CFFI extension.
requires = [
    "setuptools>=77",
    "wheel",
    "cffi>=2.0.0; python_implementation != 'PyPy'",
]
build-backend = "setuptools.build_meta"
