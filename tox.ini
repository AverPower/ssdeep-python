[tox]
envlist = py310,py311,py312,py313,py314
isolated_build = True

[testenv]
deps =
    pytest
    coverage

commands =
    coverage run --parallel-mode -m pytest --capture=no --strict {posargs}
    coverage combine
    coverage report -m
